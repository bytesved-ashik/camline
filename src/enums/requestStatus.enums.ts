export enum REQUEST_STATUS {
  IN_POOL = "in-pool",
  SCHEDULED = "scheduled",
  ACCEPTED = "accepted",
  WITHDRAWN = "withdrawn",
  EXPIRED = "expired",
  IN_SESSION = "in-session",
  ENDED = "ended",
  OPEN_SESSION = "open schedule",
}
